name: Desligar Servidor Agora
on:
  workflow_dispatch:

permissions:
  contents: write
  codespaces: write

jobs:
  stop-codespace:
    runs-on: ubuntu-latest
    steps:
      - name: Forçar Desligamento
        run: |
          # Usando o nome exato da sua imagem
          gh codespace stop -c refactored-space-happiness
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
